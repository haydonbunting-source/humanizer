name: Temporary ShareTech media transcode

on:
  pull_request:
    paths:
      - "tmp/run-sharetech-media"

permissions:
  contents: read

jobs:
  transcode:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Install FFmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Download source media
        run: |
          set -euo pipefail
          curl --fail --location --retry 4 --retry-all-errors \
            --output hero-source.mp4 \
            "https://d8j0ntlcm91z4.cloudfront.net/user_38xzZboKViGWJOttwIXH07lWA1P/hf_20260729_102822_0e6c87e8-c141-4744-bf32-ad30db296371.mp4"
          test -s hero-source.mp4

      - name: Probe source
        run: |
          mkdir -p output
          cp hero-source.mp4 output/hero-source-original.mp4
          ffprobe -v error -show_format -show_streams -of json hero-source.mp4 | tee output/source-probe.json

      - name: Encode scrub-optimized desktop master
        run: |
          ffmpeg -hide_banner -y -i hero-source.mp4 -map 0:v:0 -an \
            -c:v libx264 -preset slow -crf 11 \
            -g 1 -keyint_min 1 -sc_threshold 0 \
            -pix_fmt yuv420p -movflags +faststart \
            output/hero-scrub-1080.mp4

      - name: Encode scrub-optimized mobile master
        run: |
          ffmpeg -hide_banner -y -i hero-source.mp4 -map 0:v:0 -an \
            -vf "scale='min(1280,iw)':-2:flags=lanczos" \
            -c:v libx264 -preset slow -crf 12 \
            -g 1 -keyint_min 1 -sc_threshold 0 \
            -pix_fmt yuv420p -movflags +faststart \
            output/hero-scrub-720.mp4

      - name: Verify encoded media and visual fidelity
        run: |
          set -euo pipefail
          ffprobe -v error -show_format -show_streams -of json output/hero-scrub-1080.mp4 > output/desktop-probe.json
          ffprobe -v error -show_format -show_streams -of json output/hero-scrub-720.mp4 > output/mobile-probe.json
          ffprobe -v error -select_streams v:0 -show_entries frame=key_frame -of csv=p=0 output/hero-scrub-1080.mp4 | sort | uniq -c > output/desktop-keyframes.txt
          ffprobe -v error -select_streams v:0 -show_entries frame=key_frame -of csv=p=0 output/hero-scrub-720.mp4 | sort | uniq -c > output/mobile-keyframes.txt
          ffmpeg -hide_banner -i hero-source.mp4 -i output/hero-scrub-1080.mp4 -lavfi ssim -f null - 2> output/desktop-ssim.txt
          ffmpeg -hide_banner -i hero-source.mp4 -i output/hero-scrub-1080.mp4 -lavfi psnr -f null - 2> output/desktop-psnr.txt
          ffmpeg -hide_banner -i hero-source.mp4 -i output/hero-scrub-720.mp4 -lavfi "[0:v]scale=1280:720:flags=lanczos[ref];[ref][1:v]ssim" -f null - 2> output/mobile-ssim.txt
          sha256sum output/* > output/SHA256SUMS.txt
          grep -q '^ *241 1' output/desktop-keyframes.txt
          grep -q '^ *241 1' output/mobile-keyframes.txt
          ls -lh output

      - name: Upload optimized media
        uses: actions/upload-artifact@v4
        with:
          name: sharetech-scrub-media-verified
          path: output/
          if-no-files-found: error
          compression-level: 0
          retention-days: 1
